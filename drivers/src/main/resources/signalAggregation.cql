// create signal aggregation table
CREATE TABLE if not exists signal_aggregation (id bigint, aggregate timestamp, count bigint, sum bigint, min bigint, max bigint, avg double, PRIMARY KEY ((id), aggregate)) WITH CLUSTERING ORDER BY (aggregate DESC);