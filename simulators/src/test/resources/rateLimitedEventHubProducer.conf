# producer specific properties
producer {
  id = eventhub
}
# eventhub client specific properties from azure
eventhub {
  namespace = namespace
  # case specify - eventHub name
  name = eventhub name
  accessKeyName = mySharedAccessKeyName
  accessKey = myAccessKey
}
