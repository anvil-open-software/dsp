driver {
  # driver specific properties
  appName = CumulativeCount
}

spark {
  # driver url
  master = "local[*]"
  # location of the spark checkpoint dir
  checkpoint.dir = /Users/mitchemr/checkpoint
}